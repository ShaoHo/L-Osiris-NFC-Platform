## 2) CHANGELOG.md — 可直接貼
```md
# Changelog

## v0.1.0
- M0 baseline monorepo scaffold
- API boot (NestJS)
- Global prefix `/v1`
- Healthcheck endpoint `GET /v1/health`
- Local infra via docker-compose (Postgres + Redis)
- Prisma integrated with generate/migrate scripts